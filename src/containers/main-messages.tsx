import React, { FC } from "react";
import MainMessagesComponent from "../components/main-messages";

const MainMessages: FC = () => {
  return <MainMessagesComponent />;
};

export default MainMessages;
